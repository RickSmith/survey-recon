# Pages not yet swept to the plain-language standard.
#
# Issue #133 set the standard. Issue #134 wrote it down as a skill, at
# toolkit/.claude/skills/plain-language/, and built the test beside this file
# that counts the three rules a machine can count. This is the list of pages
# that do not meet it yet.
#
# A page named here is skipped. A page not named here is held to all three
# rules and fails the build if it breaks one.
#
# HOW THIS LIST GETS SHORTER
#
# Each child work order of #133 owns one folder. It fixes those pages and
# deletes their lines from this file, in the same pull request. Delete a line
# without fixing the page and test_every_page_not_on_the_list_is_clean catches
# it. Fix a page and forget the line and
# test_every_file_on_the_list_really_is_dirty catches that.
#
# WHEN IT IS EMPTY
#
# Delete this file and the allowlisted() function in test_plain_language.py.
# The length of this list is how much of #133 is left.
#
# WHAT IS NOT HERE, AND MUST NEVER BE
#
# docs/corridor-screen/spec.md and docs/txdot-research.md are the agent's own
# dated output. docs/agents/ is read by software.
# docs/slides/beyond-the-prompt.md is slide bullets. Those four are exempt,
# which is a decision that stands, not a debt that gets paid. They are named in
# EXEMPT in test_plain_language.py and the reason is in
# docs/adr/0003-produced-artifacts-stay-as-produced.md.
#
# Counted on 2026-09-15: 17 pages here, 41 faults between them. 25 pages in
# scope pass.
#
# #135 swept docs/data-sources/ and took its 8 pages and 39 faults off this
# list. #137 swept docs/presenting/ and took its 3 pages and 17 faults off.
# Eleven of the 25 passing pages pass because of those two.

# --- #136  docs/governance/ --- 4 pages, 9 faults
docs/governance/ai-use-policy.md
docs/governance/index.md
docs/governance/seal-and-responsible-charge.md
docs/governance/what-never-leaves.md
# Counted on 2026-09-15: 16 pages here, 49 faults between them. 26 pages in
# scope pass.
#
# #135 swept docs/data-sources/ and took its 8 pages and 39 faults off this
# list. #136 swept docs/governance/ and took its 4 pages and 9 faults off.
# Twelve of the 26 passing pages pass because of those two.
#
# #136 also changed what the counter reads. Four sentences in docs/governance/
# run past forty words because the Texas Legislature and the board wrote them
# that way, quoted in blockquotes. Shortening one would falsify a citation, so
# the counter stopped reading blockquotes. A blockquote used as a callout is
# therefore no longer counted either, and the plain-language skill says so.

# --- #138  docs/managing-your-agent/ --- 3 pages, 7 faults
docs/managing-your-agent/the-claim-we-got-wrong.md
docs/managing-your-agent/the-description-that-outlived-its-evidence.md
docs/managing-your-agent/the-superseded-manual.md

# --- #139  docs/corridor-screen/ prose --- 3 pages, 7 faults
docs/corridor-screen/index.md
docs/corridor-screen/lead-times.md
docs/corridor-screen/roe-letters.md

# --- #140  everything left over --- 7 pages, 18 faults
docs/day-0/index.md
docs/for-principals/index.md
docs/going-further/index.md
docs/plan-of-record.md
docs/scenarios/sh16/capture-note.md
docs/scenarios/sh16/index.md
docs/slides/index.md
