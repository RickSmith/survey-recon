# Provenance record. The file beside this one is exactly what TxDOT serves and
# is never edited.
#
# `source_url` was not assumed. The file was fetched from it, and the SHA-256 of
# what came back equals the SHA-256 below. `mirror_url` is TxDOT's older FTP
# host; it was fetched too and returned identical bytes, so the two hosts are
# mirrors of one document rather than two revisions of it.
#
# Do NOT cite www.dot.state.tx.us/insdtdot/... for these files. That host answers
# HTTP 200 with a "Page Not Found" HTML page for every one of them -- a soft-404,
# which is a not-found answer wearing a success status code. See
# project-sh16/manual-pulls/README.md.

document = "TCP(S-5)-08 -- Traffic Control Plan for Surveying Operations (CAD source)"
publisher = "Texas Department of Transportation, Traffic Operations Division"
standard_sheet_index_no = "215"
cases_drawn = "TCP(S-5a) work on right shoulder of divided roadways; TCP(S-5b) work on median shoulder of divided roadways"
roadway_class = "Conventional Roads Only (footnote on the sign-spacing table)"
copyright = "(C) TxDOT August 2008"
format = "MicroStation DGN (V8, OLE2 compound file)"
file = "tcp-s-5-08.dgn"
source_url = "https://ftp.txdot.gov/pub/txdot-info/cmd/cserve/standard/traffic/tcps5.dgn"
mirror_url = "https://ftp.dot.state.tx.us/pub/txdot-info/cmd/cserve/standard/traffic/tcps5.dgn"
captured_at = 2026-09-13T10:01:49-05:00
captured_by = "agent (curl)"
http_status = 200
bytes = 155136
sha256 = "492694fbcd49ed87e380dcf1cea06d5992cdf2360947a4ca15da6aba2c320917"
source_url_verified_at = 2026-09-13T10:22:00-05:00
source_url_verified_by = "sha256 of fetched bytes equals sha256 of committed file"
mirror_url_verified = true
